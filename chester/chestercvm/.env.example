# Server private key (hex format, 32 bytes)
# If not set, one will be generated and persisted to .env
SERVER_PRIVATE_KEY=

# Comma-separated list of relay URLs
RELAYS=wss://relay.contextvm.org,wss://cvm.otherstuff.ai
